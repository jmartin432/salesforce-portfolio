import { LightningElement } from 'lwc';

/**
 * @slot header This is the header slot
 * @slot main This is the main slot
 * @slot footer This is the footer slot
 */

export default class PortfolioLayout extends LightningElement {}